---
interface Props {
  date: Date
}

const { date } = Astro.props
---

<time class="font-semibold" datetime={date.toISOString()}>
  {
    date.toLocaleDateString(undefined, {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
